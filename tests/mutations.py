"""mutgate's own contracts, checked by mutgate in CI (the tool eating its own cooking)."""

from mutgate import Mutation

TESTS = ["tests/test_mutgate.py"]
PATHS = ["src"]

MUTATIONS = [
    Mutation("apply-ignores-count", "src/mutgate/core.py",
             old="        if found != m.count:", new="        if found == 0:",
             fires=("test_not_applied_when_old_is_absent_or_ambiguous",),
             note="an ambiguous `old` would be replaced everywhere and reported as applied"),
    Mutation("overreach-is-ok", "src/mutgate/core.py",
             old='        return Verdict(m, "OVERREACH", fired_t, missing, unexpected)',
             new='        return Verdict(m, "OK", fired_t, missing, unexpected)',
             fires=("test_overreach_is_the_two_copies_tell",),
             may_fire=("test_a_project_that_stops_at_the_first_failure_still_yields_the_whole_set",),
             note="the maxfail tests assert OVERREACH too, so they detect this as well"),
    Mutation("invisible-never-visible", "src/mutgate/core.py",
             old='        return Verdict(m, "VISIBLE" if fired else "OK", fired_t)',
             new='        return Verdict(m, "OK", fired_t)',
             fires=("test_visible_when_an_invariance_breaks",),
             may_fire=("test_table_renders_every_verdict_kind",)),
    Mutation("baseline-not-checked", "src/mutgate/core.py",
             old="        if failed:\n            report.baseline_failed = tuple(failed)\n            return report",
             new="        if False:\n            report.baseline_failed = tuple(failed)\n            return report",
             fires=("test_red_baseline_aborts_before_any_mutation",),
             may_fire=("TestVerdicts", "test_run_exit_2_paths")),
    Mutation("no-restore", "src/mutgate/core.py",
             old="                    sb.restore(m, original)", new="                    pass",
             fires=("test_working_tree_is_untouched_and_restored_between_mutations",),
             may_fire=("test_cli_run_and_list", "test_run_plain_and_markdown",
                       "test_relative_python_resolves_from_the_callers_cwd",
                       "test_a_relative_declared_python_is_anchored_at_the_root"),
             note="the CLI tests run two mutations in sequence and see the unrestored file too"),
    Mutation("error-reads-as-fired", "src/mutgate/core.py",
             old="    if rc not in (0, 1):\n        # only 0 and 1", new="    if False:\n        # only 0 and 1",
             fires=("test_error_when_the_mutation_does_not_compile",),
             may_fire=("test_an_interrupted_run_with_a_partial_fired_set_is_an_error",),
             note="the interrupted-run test goes through the same branch and detects this too"),
    # the review of the first commit (2026-09-07): three silent false passes in the checker
    Mutation("summary-gate-off", "src/mutgate/core.py",
             old="    if rc == 1 and not fired:\n        # the gate", new="    if False:\n        # the gate",
             fires=("test_suppressed_summary_is_an_error_not_a_verdict",),
             note="review item 2: pytest exit 1 with nothing readable must be ERROR"),
    Mutation("string-fires-accepted", "src/mutgate/core.py",
             old="            if isinstance(val, str):", new="            if False:",
             fires=("test_a_bare_string_or_empty_fragment_is_refused",),
             note="review item 3: a bare string would match everything"),
    Mutation("only-typo-ignored", "src/mutgate/core.py",
             old="        if unknown:\n            raise ValueError", new="        if False:\n            raise ValueError",
             fires=("test_unknown_only_name_and_empty_list_are_errors",),
             may_fire=("test_cli_run_and_list", "test_run_exit_2_paths")),
    Mutation("sandbox-dir-unresolved", "src/mutgate/core.py",
             old='        self.dir = Path(tempfile.mkdtemp(prefix="mutgate-")).resolve()',
             new='        self.dir = Path(tempfile.mkdtemp(prefix="mutgate-"))',
             fires=("test_sandbox_survives_a_symlinked_tmpdir",),
             note="review item 1: a symlinked TMPDIR failed the containment check"),
    Mutation("maxfail-not-owned", "src/mutgate/core.py",
             old='           *extra_args, "--maxfail=0", *tests]', new='           *extra_args, *tests]',
             fires=("test_a_project_that_stops_at_the_first_failure_still_yields_the_whole_set",),
             note="review round 2: a project's -x truncates the fired set with a consistent exit"),
    Mutation("cut-short-tell-ignored", "src/mutgate/core.py",
             old='    if _CUT_SHORT_LINE.search(proc.stdout):', new='    if False:',
             fires=("test_a_conftest_that_forces_maxfail_is_an_error",)),
    Mutation("interrupted-with-partial-set-is-a-verdict", "src/mutgate/core.py",
             old="    if rc not in (0, 1):\n        # only 0 and 1", new="    if rc not in (0, 1) and not fired:\n        # only 0 and 1",
             fires=("test_an_interrupted_run_with_a_partial_fired_set_is_an_error",),
             note="review round 3: exit 2 with one failure parsed read OK"),
    # the first two issues, filed from another repo's first use (2026-09-07)
    Mutation("python-unresolved", "src/mutgate/cli.py",
             old="    python = os.path.abspath(found)\n", new="",
             fires=("test_relative_python_resolves_from_the_callers_cwd",),
             note="issue #1: a relative --python passed the check, then crashed in the sandbox"),
    Mutation("keep-ignored", "src/mutgate/cli.py",
             old="keep=args.keep,", new="keep=False,",
             fires=("test_run_verbose_only_keep_and_stop",),
             note="issue #2: the test asserted the word 'sandbox', not that the directory was kept"),
    Mutation("declared-python-cwd-anchored", "src/mutgate/core.py",
             old="        python = str(root / python)", new="        pass",
             fires=("test_a_relative_declared_python_is_anchored_at_the_root",),
             note="PR #3 review: a relative PYTHON is the project's, like ROOT and PATHS"),
]
